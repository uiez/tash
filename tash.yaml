tasks:
  build:
    description: |-
      build native binary
    actions:
      cmd:
        exec: go build -ldflags "-w -s"

  watch:
    description: |-
      watch fs changes and build native binary
    actions:
      watch:
        dirs: .
        files: "*.go"
        actions:
          task:
            name: native